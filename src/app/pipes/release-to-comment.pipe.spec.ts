import { ReleaseToCommentPipe } from './release-to-comment.pipe';

describe('ReleaseToCommentPipe', () => {
  it('create an instance', () => {
    const pipe = new ReleaseToCommentPipe();
    expect(pipe).toBeTruthy();
  });
});
